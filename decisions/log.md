# Decisions Log

## 2026-06-14 — Astro scaffold (lift-and-shift, Phase 1)

Installed Astro v6 (static output, `build.format: 'file'`) and converted all 14 HTML pages to `.astro` equivalents in `src/pages/`. Assets (css/, js/, images/, assets/) moved to `public/`. Internal href links converted to absolute extensionless paths; local image `src` and `url()` values converted to root-relative `/images/...`; `<script src>` changed to `<script is:inline src="/js/main.js">`. npm scripts use `node ./node_modules/astro/bin/astro.mjs …` directly to avoid a Windows CMD bug where the `&` in the project directory name breaks npm's `.cmd` wrapper. Original `.html` files kept in place; they no longer render standalone (broken CSS paths after asset move) but are preserved until explicitly deleted.

## 2026-06-14 — Homepage content + card-consistency pass (`src/pages/index.astro` only)

Rewrote the homepage in one pass — the file was riddled with curly quotes inside `class`/`style`/`href` attributes, which silently broke the Greece, Kenya, and Provence cards (and two testimonial credits), making their links fall back to default browser blue. Replaced all curly quotes with straight quotes; the terracotta `.card-link` color was already correct in CSS, so no stylesheet change was needed. Hero headline → "Retreats in the world's most beautiful places." (with italic `<em>` accent). Brown-band `.manifesto-text` → "Nervous system reset. / This is where body, mind, and soul come home." (split to keep the gold `<strong>` accent); `.manifesto-sub` left unchanged. Trimmed `.why-us` inline padding 80→48px so the short section hugs its content. Unified all 7 retreat cards to the image-card style: Greece uses local `greece-peloponnese-coast.jpg`, Provence uses local `provence-lavender-field.jpg`, Kenya uses a solid `var(--terra)` fallback banner (no new CSS). Per content rule, only Greece keeps a date + "Retreat Details" link; the other six (Kenya, Egypt, Morocco, Provence, Geneva, Exotic) lost both. Motif fixes to lowercase: Provence "SoFen"→"savor", Morocco "Rooted"→"rooted"; Egypt given motif "drift" per request. "What's included" rebuilt to six tiles (Daily Yoga & Barre · Nourishing Meals · Sound Baths · Breathwork & Meditation · Excursions & Experiences · Small Group Setting), reusing existing copy/icons where available and adapting the Sound and Community SVGs to the 24px include-icon style. Testimonial credits standardized to "Retreat Guest · {Place} · {Year}". Newsletter section removed (main.js handles its absence safely). Build passes; zero curly quotes remain.

## 2026-06-15 — Retreat detail pages → "coming soon" layout (Kenya, Egypt, Morocco, Provence, Geneva)

Converted five not-yet-dated retreat detail pages to mirror the Exotic template so unannounced retreats read as intentional "coming soon" pages rather than showing placeholder itineraries with invented dates. Per page: removed the fixed hero date badge (kept the hero image, region eyebrow, serif name, and SolScape watermark); replaced the two-column overview + "Retreat at a Glance" facts sidebar with Exotic's centered Coming Soon column (eyebrow → headline with `<em>` accent → divider → one paragraph → italic motif); added the shared "Private Retreats — available upon request" charcoal band (per founder decision, full Exotic parity); and swapped the inquiry form to Exotic's "Register Interest" copy while preserving each page's own Netlify form name (`inquiry-<page>`, never `inquiry-exotic`). Morocco/Provence/Geneva also had their detailed "Included & not included" section removed — a fixed inclusions list contradicts "details coming soon" — which also retired Provence's stray "SoFen" motif (→ savor). Egypt's hero image was broken (404 → grey gradient); repointed it at the same Nile-felucca Unsplash photo the homepage Egypt card uses (note: no local repo image exists for Egypt, so this is a remote URL, consistent with the homepage). Greece, Exotic, and `past/*` left untouched. Build passes (14 pages); zero curly quotes.

## 2026-06-15 — Greece (Mani Edition) detail page rebuilt with final client content (`src/pages/retreats/greece.astro` only)

Replaced the placeholder Greece content with the client's final Mani Edition copy — a content + layout pass, no redesign, reusing existing CSS classes only (zero stylesheet changes). Hero headline changed "Monemvasia Retreat" → "Mani Retreat" (the accommodation, Brazzo di Maina, is on the Mani peninsula, not Monemvasia — confirmed with founder); date/price badge now "September 27 – October 1, 2026 · 4 Nights · From €1,590 pp". Overview now carries the full client paragraph (all sentences) and the facts-card was refreshed (new dates, Mani location, "From €1,590 pp sharing", status "Now open for booking"); the "Group Size" fact was dropped (no number to show). Added four new sections built from existing components: **The Experience** (spaced "Move · Rest · Explore · Reconnect" line + five `.offering-card` blocks, text-only as photo slots for the next pass), **Your Hosts** (charcoal accent band, Nada & Samantha paragraph — chosen partly to keep the cream/linen background rhythm alternating cleanly), **Accommodation & Pricing** (four `.offering-card` room types with prices, `.tag` room counts, and the first-come/first-served note), and **Payment & Booking** (terms paragraph + "Now open for booking" + CTA). Included/Not-Included lists swapped to the final content. The existing Netlify `inquiry-greece` form is kept as the booking action (only its retreat `<option>` label updated Oct → Sept 2026). Images, other retreat pages, homepage, About, nav, and footer untouched; testimonials kept off the page. Build passes (14 pages); straight quotes in all attributes. Not yet deployed — awaiting approval.

## 2026-06-15 — Greece (Mani Edition) refinements: form removed for direct contact (`src/pages/retreats/greece.astro` only)

Round of small content/layout fixes plus retiring the on-page form. Hero `.dates-badge` trimmed to "September 27 – October 1, 2026 · 4 Nights" (dropped the "From €1,590 pp" segment, which duplicated pricing already shown in the glance box and rooms). Glance-box FROM row widened to a range, "€1,590 pp sharing" → "€1,590 – €2,690 pp", spanning the cheapest sharing rate to the top Executive exclusive rate. Removed "Alcoholic beverages" from the Not Included column (founder request). First-come note spacing increased (margin-top 28→64px) so it centers in the gap below the room cards instead of hugging them. **Removed the entire Express Interest Netlify form section (`#inquiry`, `inquiry-greece`)** — the brand is inquiry-based with no checkout, so the Payment & Booking section's single "Enquire to Book" button was replaced with two site-style contact buttons: "Message us on Instagram" (`.btn-primary` → `https://ig.me/m/solscape_retreats`, the exact DM link used by nav/footer) and "Email us" (`.btn-outline` → `mailto:solscaperetreats@gmail.com`, the site's existing address). The glance-box "Enquire Now" button was repointed from the now-deleted `#inquiry` to a new `id="booking"` on the Payment & Booking section (no dead anchor). To keep background alternation after losing the linen form band, the bottom Other Retreats strip was flipped cream → linen (final order: cream → linen → charcoal → cream → linen → cream → linen, no adjacent repeats). Build passes (14 pages); straight quotes throughout. Backlog captured below. Not yet deployed.

### Backlog — Greece (for later passes, not built now)

- **Room gallery:** each room card to get a "View Room" button opening that suite's photos + room details. Build during the Greece photo pass once room photos are supplied.
- **Inquiry destination:** decide at Netlify deploy step — Netlify Forms (free; dashboard + email) optionally mirrored to Airtable or a Google Sheet so founders can view and follow up. No custom CRM.

## 2026-06-15 — Coming-soon pages: forms removed for direct contact, strip-dates trimmed (Kenya, Egypt, Morocco, Provence, Geneva, Exotic)

Applied the Greece contact-button treatment across all six coming-soon / private-retreat pages. On each, the charcoal "Private Retreats — available upon request" band's single "Get in Touch" button (→ /contact) was replaced with the same two contact buttons used on Greece: "Message us on Instagram" (`.btn-primary` → `https://ig.me/m/solscape_retreats`) and "Email us" (→ the Gmail compose URL `https://mail.google.com/mail/?view=cm&fs=1&to=solscaperetreats@gmail.com`), both opening in a new tab, wrapped in a centered flex row. Critically, the secondary button uses `.btn-ghost` (white text + white border) rather than Greece's `.btn-outline` — `.btn-outline`'s `var(--mocha)` text is dark-on-dark and would be illegible on the charcoal band; `.btn-ghost` is the site's existing inverse button for dark sections, so no new CSS. The entire "Register Interest" Netlify form section (`inquiry-<slug>`) was removed from all six — section rhythm stays clean (Overview cream → Private Retreats charcoal → Other Retreats cream, no adjacent repeats), so no background fix was needed. In each bottom "Explore other retreats" strip, specific dates were stripped to "Upcoming" on every card except the Greece card (keeps "October 4–8, 2026") and the Exotic card (keeps "Coming Soon"), per the established "only Greece shows a confirmed date" rule; motifs (incl. the stray Provence "SoFen"), descriptions, links, and images were left untouched. Greece, `past/*`, homepage, About, contact, nav, and footer untouched. Build passes (14 pages); straight quotes throughout. Not yet deployed.

**Decision:** Register Interest form removed from all coming-soon/private-retreat pages for now (liked — reintroduce once inquiry destination is decided at deploy, same as the Greece form decision).

## 2026-06-16 — About page: Nada's founder card fixed + Philosophy band restored (`src/pages/about.astro` only)

About page — fixed Nada's broken founder card (curly-quote bug from the original HTML), rebuilt it to match Samantha's structure, pointed both founders at the new baked-in portraits (nada-hosts.png / samantha-hosts.png), and restored the Philosophy band's two supporting paragraphs (also broken by curly quotes). No git.
